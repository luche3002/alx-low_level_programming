#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: integer to be checked
 *
 * Return: -i or i
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
